name: Temporary Phone QuickCheck foldable audit wave 3

on:
  push:
    branches:
      - fix/phone-quickcheck-foldable-audit-wave3-20260915
    paths:
      - .github/workflows/tmp-phone-quickcheck-foldable-audit-wave3.yml

permissions:
  contents: write

jobs:
  apply-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/phone-quickcheck-foldable-audit-wave3-20260915
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node scripts/tmp-phone-quickcheck-foldable-audit-wave3.mjs
      - run: node scripts/check-phone-quickcheck-data.mjs
      - run: node scripts/check-phone-quickcheck-source-semantics.mjs
      - run: node scripts/check-phone-quickcheck-affiliate.mjs
      - run: node tools/phone-quickcheck/tests/behavior.test.mjs
      - name: Commit validated canonical file
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/phone-quickcheck/data/phones.json
          git diff --cached --check
          git commit -m "Phone QuickCheck: verify Motorola foldable package contents"
          git push origin HEAD:fix/phone-quickcheck-foldable-audit-wave3-20260915
