name: Temp Phone QuickCheck foldable gap audit

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

jobs:
  audit:
    if: github.event.pull_request.head.ref == 'audit/phone-quickcheck-foldable-gaps-20260915'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Audit foldable canonical gaps
        run: node scripts/temp-audit-phone-quickcheck-foldables.mjs
