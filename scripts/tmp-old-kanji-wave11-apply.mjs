import fs from 'node:fs';
import path from 'node:path';

const root = process.cwd();
const branchFiles = {
  modernizerApp: path.join(root, 'tools', 'kanji-modernizer', 'app.js'),
  referenceApp: path.join(root, 'tools', 'old-kanji-reference', 'app-meaning-v4.js'),
  referenceHtml: path.join(root, 'tools', 'old-kanji-reference', 'index.html'),
};

function mustReplace(file, before, after, label) {
  const current = fs.readFileSync(file, 'utf8');
  if (!current.includes(before)) throw new Error(`Missing replacement anchor: ${label}`);
  const next = current.replace(before, after);
  if (next === current) throw new Error(`Replacement made no change: ${label}`);
  fs.writeFileSync(file, next);
}

// Kanji Modernizer: preserve user text boundaries and wait for dictionary readiness before ?q= auto-convert.
mustReplace(
  branchFiles.modernizerApp,
  `  function setProgress(active) {\n    const bar = document.getElementById("progress");\n    if (!bar) return;\n    bar.style.display = active ? "block" : "none";\n  }\n`,
  `  function setProgress(active) {\n    const bar = document.getElementById("progress");\n    if (!bar) return;\n    bar.style.display = active ? "block" : "none";\n  }\n\n  function prepareInputText(value) {\n    const text = typeof value === "string" ? value : "";\n    return { text, hasContent: text.trim().length > 0 };\n  }\n`,
  'modernizer prepareInputText',
);

mustReplace(
  branchFiles.modernizerApp,
  `    const initDict = async () => {\n      setConvertEnabled(false);\n      try {\n        const dict = await loadDict();\n        updateCounts(dict);\n        clearMessage();\n        setConvertEnabled(true);\n      } catch (err) {\n        console.error(err);\n        showMessage("loadError", { showRetry: true, showNote: true, type: "error" });\n        setConvertEnabled(false);\n      }\n    };\n\n    initDict();\n\n    if (retryBtn) {\n      retryBtn.addEventListener("click", () => {\n        dictCache = null;\n        initDict();\n      });\n    }\n\n    if (convertBtn) {\n      convertBtn.addEventListener("click", async () => {\n        clearMessage();\n        const text = (input && input.value ? input.value : "").trim();\n        if (!text) {\n          showMessage("empty", { type: "error" });\n          return;\n        }\n\n        setProgress(true);\n        try {\n          const dict = await loadDict();\n          const selected = document.querySelector('input[name="direction"]:checked');\n          const direction = selected ? selected.value : "old-to-new";\n          const exclude = excludeToggle ? excludeToggle.checked : false;\n          const policyRadio = document.querySelector('input[name="conversionPolicy"]:checked');\n          const policy = policyRadio ? policyRadio.value : "conservative";\n          const result = convertText(text, direction, dict, { exclude, policy });\n\n          lastResultText = result.plain;\n          lastReplacementList = result.replacements;\n          lastAmbiguityList = result.ambiguities;\n          if (inputHighlight) inputHighlight.innerHTML = result.inputHtml;\n          if (output) output.innerHTML = result.outputHtml;\n          renderReplacementTable(result.replacements);\n          renderAmbiguityReview(result.ambiguities);\n          if (resultBlock) resultBlock.hidden = false;\n        } catch (e) {\n          console.error(e);\n          showMessage("loadError", { showRetry: true, showNote: true, type: "error" });\n        } finally {\n          setProgress(false);\n        }\n      });\n    }\n    if (qParam && convertBtn) convertBtn.click();\n`,
  `    let pendingAutoConvert = Boolean(qParam);\n\n    const runConversion = async () => {\n      clearMessage();\n      const prepared = prepareInputText(input ? input.value : "");\n      if (!prepared.hasContent) {\n        showMessage("empty", { type: "error" });\n        return;\n      }\n\n      setProgress(true);\n      try {\n        const dict = await loadDict();\n        const selected = document.querySelector('input[name="direction"]:checked');\n        const direction = selected ? selected.value : "old-to-new";\n        const exclude = excludeToggle ? excludeToggle.checked : false;\n        const policyRadio = document.querySelector('input[name="conversionPolicy"]:checked');\n        const policy = policyRadio ? policyRadio.value : "conservative";\n        const result = convertText(prepared.text, direction, dict, { exclude, policy });\n\n        lastResultText = result.plain;\n        lastReplacementList = result.replacements;\n        lastAmbiguityList = result.ambiguities;\n        if (inputHighlight) inputHighlight.innerHTML = result.inputHtml;\n        if (output) output.innerHTML = result.outputHtml;\n        renderReplacementTable(result.replacements);\n        renderAmbiguityReview(result.ambiguities);\n        if (resultBlock) resultBlock.hidden = false;\n      } catch (e) {\n        console.error(e);\n        showMessage("loadError", { showRetry: true, showNote: true, type: "error" });\n      } finally {\n        setProgress(false);\n      }\n    };\n\n    const initDict = async () => {\n      setConvertEnabled(false);\n      try {\n        const dict = await loadDict();\n        updateCounts(dict);\n        clearMessage();\n        setConvertEnabled(true);\n        if (pendingAutoConvert && convertBtn) {\n          pendingAutoConvert = false;\n          Promise.resolve().then(() => { if (!convertBtn.disabled) convertBtn.click(); });\n        }\n        return true;\n      } catch (err) {\n        console.error(err);\n        showMessage("loadError", { showRetry: true, showNote: true, type: "error" });\n        setConvertEnabled(false);\n        return false;\n      }\n    };\n\n    initDict();\n\n    if (retryBtn) {\n      retryBtn.addEventListener("click", () => {\n        dictCache = null;\n        initDict();\n      });\n    }\n\n    if (convertBtn) {\n      convertBtn.addEventListener("click", runConversion);\n    }\n`,
  'modernizer conversion and q handoff',
);

mustReplace(
  branchFiles.modernizerApp,
  `        lastAmbiguityList = [];\n        clearMessage();\n`,
  `        lastAmbiguityList = [];\n        updateReferenceLink("");\n        if (window.history && typeof window.history.replaceState === "function" && window.location?.href) {\n          const cleanUrl = new URL(window.location.href);\n          cleanUrl.searchParams.delete("q");\n          window.history.replaceState(null, "", cleanUrl.pathname + cleanUrl.search + cleanUrl.hash);\n        }\n        clearMessage();\n`,
  'modernizer reset handoff state',
);

// Old Kanji Reference: O(text + entries) detector, exact text handoff, truthful export status, and retryable primary-data load.
mustReplace(
  branchFiles.referenceApp,
  `  let entriesCache = [];\n`,
  `  let entriesCache = [];\n  let oldEntryLookup = new Map();\n`,
  'reference old entry lookup declaration',
);

mustReplace(
  branchFiles.referenceApp,
  `  function buildEntries(dict){ const popularOrder = getPopularOrder(); entriesCache = Object.entries(dict.old_to_new || {}).map(([oldChar, newChar]) => { const meta = getMeta(oldChar); const newText = Array.isArray(newChar) ? newChar.join("、") : String(newChar || ""); return { oldChar, newText, oldCode: getCodePoints(oldChar), newCode: getCodePoints(newText), category: getCategory(oldChar), verified: Boolean(meta.verified), readingJa: meta.readingJa || "", readingEn: meta.readingEn || "", meaningJa: meta.meaningJa || "", meaningEn: meta.meaningEn || "", usageJa: meta.usageJa || "", usageEn: meta.usageEn || "" }; }); entriesCache.sort((a,b) => { const ia = popularOrder.indexOf(a.oldChar); const ib = popularOrder.indexOf(b.oldChar); if (ia >= 0 && ib >= 0) return ia - ib; if (ia >= 0) return -1; if (ib >= 0) return 1; return a.oldChar.localeCompare(b.oldChar, "ja"); }); }\n`,
  `  function buildEntries(dict){ const popularOrder = getPopularOrder(); entriesCache = Object.entries(dict.old_to_new || {}).map(([oldChar, newChar]) => { const meta = getMeta(oldChar); const newText = Array.isArray(newChar) ? newChar.join("、") : String(newChar || ""); return { oldChar, newText, oldCode: getCodePoints(oldChar), newCode: getCodePoints(newText), category: getCategory(oldChar), verified: Boolean(meta.verified), readingJa: meta.readingJa || "", readingEn: meta.readingEn || "", meaningJa: meta.meaningJa || "", meaningEn: meta.meaningEn || "", usageJa: meta.usageJa || "", usageEn: meta.usageEn || "" }; }); entriesCache.sort((a,b) => { const ia = popularOrder.indexOf(a.oldChar); const ib = popularOrder.indexOf(b.oldChar); if (ia >= 0 && ib >= 0) return ia - ib; if (ia >= 0) return -1; if (ib >= 0) return 1; return a.oldChar.localeCompare(b.oldChar, "ja"); }); oldEntryLookup = new Map(entriesCache.map(entry => [entry.oldChar, entry])); }\n`,
  'reference build lookup',
);

mustReplace(
  branchFiles.referenceApp,
  `  function detectOldForms(text){\n    const counts = new Map();\n    Array.from(String(text || "")).forEach((ch) => { if (entriesCache.some(entry => entry.oldChar === ch)) counts.set(ch, (counts.get(ch) || 0) + 1); });\n    return entriesCache.filter(entry => counts.has(entry.oldChar)).map(entry => ({ entry, count: counts.get(entry.oldChar) }));\n  }\n`,
  `  function detectOldForms(text){\n    const counts = new Map();\n    Array.from(String(text || "")).forEach((ch) => { if (oldEntryLookup.has(ch)) counts.set(ch, (counts.get(ch) || 0) + 1); });\n    return entriesCache.filter(entry => counts.has(entry.oldChar)).map(entry => ({ entry, count: counts.get(entry.oldChar) }));\n  }\n`,
  'reference detector complexity',
);

mustReplace(
  branchFiles.referenceApp,
  `  function sendDetectorTextToConverter(){\n    const input = document.getElementById("detectorInput");\n    const text = (input?.value || "").trim();\n    if (!text) {\n      showToast(messages[currentLang].noDetectorTextToSend);\n      return;\n    }\n    window.location.href = toConverterUrl(text);\n  }\n`,
  `  function sendDetectorTextToConverter(){\n    const input = document.getElementById("detectorInput");\n    const text = input?.value || "";\n    if (!text.trim()) {\n      showToast(messages[currentLang].noDetectorTextToSend);\n      return;\n    }\n    window.location.href = toConverterUrl(text);\n  }\n`,
  'reference detector handoff boundaries',
);

mustReplace(
  branchFiles.referenceApp,
  `  function getDataStatus(entry){ return hasMeaning(entry) ? "verified" : "pair-only"; }\n`,
  `  function getDataStatus(entry){ return entry.verified ? "verified" : "pair-only"; }\n`,
  'reference export verification status',
);

mustReplace(
  branchFiles.referenceHtml,
  `      <div id="statusMessage" class="status-message" role="status"></div>\n`,
  `      <div id="statusMessage" class="status-message" role="status"></div>\n      <button id="referenceRetryBtn" class="copy-btn" type="button" hidden><span data-i18n="ja">辞書を再読み込み</span><span data-i18n="en">Retry dictionary load</span></button>\n`,
  'reference retry control',
);

mustReplace(
  branchFiles.referenceApp,
  `    syncOkjProRuntimeState();\n    switchLang(currentLang); setStatusText(messages[currentLang].loading); loadData().then(dict => { loadFailed = false; setCounts(dict); buildEntries(dict); makeQuizQuestion(); renderAll(); }).catch(() => { loadFailed = true; renderAll(); });\n`,
  `    const referenceRetryBtn = document.getElementById("referenceRetryBtn");\n    const loadReferenceData = () => {\n      if (referenceRetryBtn) referenceRetryBtn.hidden = true;\n      setStatusText(messages[currentLang].loading);\n      return loadData().then(dict => {\n        loadFailed = false;\n        setCounts(dict);\n        buildEntries(dict);\n        makeQuizQuestion();\n        renderAll();\n        return true;\n      }).catch(() => {\n        loadFailed = true;\n        renderAll();\n        if (referenceRetryBtn) referenceRetryBtn.hidden = false;\n        return false;\n      });\n    };\n    if (referenceRetryBtn) referenceRetryBtn.addEventListener("click", loadReferenceData);\n    syncOkjProRuntimeState();\n    switchLang(currentLang);\n    loadReferenceData();\n`,
  'reference retryable load',
);

const referenceTestDir = path.join(root, 'tools', 'old-kanji-reference', 'tests');
const modernizerTestDir = path.join(root, 'tools', 'kanji-modernizer', 'tests');
fs.mkdirSync(referenceTestDir, { recursive: true });
fs.mkdirSync(modernizerTestDir, { recursive: true });

fs.writeFileSync(path.join(referenceTestDir, 'behavior.test.mjs'), `import assert from 'node:assert/strict';\nimport fs from 'node:fs';\nimport vm from 'node:vm';\n\nlet source = fs.readFileSync(new URL('../app-meaning-v4.js', import.meta.url), 'utf8');\nsource = source.replace(/\\n\\}\\)\\(\\);\\s*$/, \\`\\n  globalThis.__test = {\n    getCodePoints, buildEntries, getFilteredEntries, detectOldForms, buildExportRows, csvEscape, toConverterUrl,\n    loadStoredList, isValidDisplayMode, loadData,\n    setMeta(entries, popularOrder = []) { metaCache = { entries, popularOrder }; },\n    setSearch({ query = '', mode = 'all', filter = 'all', preset = '' } = {}) { currentQuery = query; currentSearchMode = mode; currentFilter = filter; activePreset = preset; },\n    getEntries() { return entriesCache; }\n  };\n})();\\n\\`);\n\nconst storage = new Map();\nlet fetchImpl = async () => ({ ok: false, async json() { return {}; } });\nconst sandbox = {\n  console,\n  document: {\n    documentElement: { lang: 'ja' },\n    addEventListener() {},\n    querySelectorAll() { return []; },\n    querySelector() { return null; },\n    getElementById() { return null; },\n    createElement() { return {}; },\n    body: { appendChild() {} },\n    execCommand() { return true; },\n  },\n  window: { innerWidth: 1024 },\n  navigator: { clipboard: { async writeText() {} } },\n  localStorage: {\n    getItem(key) { return storage.has(key) ? storage.get(key) : null; },\n    setItem(key, value) { storage.set(key, String(value)); },\n  },\n  fetch: (...args) => fetchImpl(...args),\n  URLSearchParams, URL, Blob, Date, setTimeout, clearTimeout,\n};\nsandbox.window.window = sandbox.window;\nconst context = vm.createContext(sandbox);\nvm.runInContext(source, context, { filename: 'tools/old-kanji-reference/app-meaning-v4.js' });\nconst api = context.__test;\nassert.ok(api, 'reference test exports should be available');\n\napi.setMeta({\n  '舊': { verified: true, readingJa: 'きゅう', readingEn: 'kyu', meaningJa: '古い', meaningEn: 'old', category: 'document' },\n  '學': { verified: false, readingJa: 'がく', readingEn: 'gaku', meaningJa: '学ぶ', meaningEn: 'study', category: 'common' },\n});\napi.buildEntries({ old_to_new: { '舊': '旧', '學': '学', '神': '神' } });\nassert.equal(api.getEntries().length, 3);\n\napi.setSearch({ query: '舊', mode: 'old' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['舊']);\napi.setSearch({ query: '学', mode: 'new' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['學']);\napi.setSearch({ query: 'きゅう', mode: 'reading' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['舊']);\napi.setSearch({ query: 'old', mode: 'meaning' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['舊']);\napi.setSearch({ query: api.getCodePoints('舊'), mode: 'unicode' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['舊']);\napi.setSearch({ filter: 'verified' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['舊']);\napi.setSearch({ filter: 'pairOnly' });\nassert.deepEqual(Array.from(api.getFilteredEntries(), e => e.oldChar), ['神']);\n\nconst detected = api.detectOldForms('A舊舊學B');\nassert.deepEqual(detected.map(item => [item.entry.oldChar, item.count]), [['舊', 2], ['學', 1]]);\nconst largeDetected = api.detectOldForms('舊'.repeat(10000) + '學');\nassert.equal(largeDetected.find(item => item.entry.oldChar === '舊').count, 10000);\nassert.equal(largeDetected.find(item => item.entry.oldChar === '學').count, 1);\n\nconst exported = api.buildExportRows(api.getEntries());\nassert.equal(exported.find(row => row.old === '舊').dataStatus, 'verified');\nassert.equal(exported.find(row => row.old === '學').dataStatus, 'pair-only', 'unverified metadata must not be exported as verified');\nassert.equal(api.csvEscape('a,b'), '"a,b"');\nassert.equal(api.csvEscape('a"b'), '"a""b"');\n\nconst handoffText = '  舊\\n學  ';\nconst handoff = new URL(api.toConverterUrl(handoffText), 'https://nicheworks.app/tools/old-kanji-reference/');\nassert.equal(handoff.pathname, '/tools/kanji-modernizer/');\nassert.equal(handoff.searchParams.get('q'), handoffText, 'Reference → Modernizer handoff must preserve boundary whitespace/newlines');\n\nstorage.set('valid-list', JSON.stringify(['舊', '', 3, '學']));\nassert.deepEqual(Array.from(api.loadStoredList('valid-list')), ['舊', '學']);\nstorage.set('bad-list', '{not json');\nassert.deepEqual(Array.from(api.loadStoredList('bad-list')), []);\nassert.equal(api.isValidDisplayMode('compact'), true);\nassert.equal(api.isValidDisplayMode('table'), true);\nassert.equal(api.isValidDisplayMode('broken'), false);\n\nfetchImpl = async (url) => {\n  if (url === './dict.json') return { ok: false, async json() { return {}; } };\n  return { ok: true, async json() { return { entries: {} }; } };\n};\nawait assert.rejects(() => api.loadData(), /Failed to load \.\/dict\.json/);\n\nfetchImpl = async (url) => {\n  if (url === './dict.json') return { ok: true, async json() { return { old_to_new: { '舊': '旧' }, new_to_old: { '旧': ['舊'] } }; } };\n  if (String(url).startsWith('./meta.json')) return { ok: true, async json() { return { entries: {}, popularOrder: [] }; } };\n  return { ok: false, async json() { return {}; } };\n};\nconst optionalFailureDict = await api.loadData();\nassert.equal(optionalFailureDict.old_to_new['舊'], '旧', 'optional metadata failures must not block the primary dictionary');\n\nconst indexHtml = fs.readFileSync(new URL('../index.html', import.meta.url), 'utf8');\nassert.match(indexHtml, /id="referenceRetryBtn"/, 'primary dictionary failure must expose an explicit retry control');\nassert.match(source, /referenceRetryBtn\.addEventListener\("click", loadReferenceData\)/);\n\nconsole.log('Old Kanji Reference behavior test passed.');\n`);

fs.writeFileSync(path.join(modernizerTestDir, 'behavior.test.mjs'), `import assert from 'node:assert/strict';\nimport fs from 'node:fs';\nimport vm from 'node:vm';\n\nlet source = fs.readFileSync(new URL('../app.js', import.meta.url), 'utf8');\nsource = source.replace(/\\n\\}\\)\\(\\);\\s*$/, \\`\\n  globalThis.__test = {\n    rebuildDict, buildExclusionRanges, buildSegments, pickMappedChar, convertText, prepareInputText, loadDict,\n    resetDictCache() { dictCache = null; },\n    setLang(lang) { currentLang = lang; }\n  };\n})();\\n\\`);\n\nlet fetchImpl = async () => ({ ok: false, async json() { return {}; } });\nconst sandbox = {\n  console,\n  document: {\n    documentElement: { lang: 'ja' },\n    addEventListener() {},\n    querySelectorAll() { return []; },\n    querySelector() { return null; },\n    getElementById() { return null; },\n    createElement() { return { style: {}, setAttribute() {}, select() {}, remove() {} }; },\n    body: { appendChild() {} },\n    execCommand() { return true; },\n  },\n  navigator: { language: 'ja', clipboard: { async writeText() {} } },\n  window: { isSecureContext: true },\n  localStorage: { getItem() { return null; }, setItem() {} },\n  fetch: (...args) => fetchImpl(...args),\n  URLSearchParams, URL, Promise, encodeURIComponent, setTimeout, clearTimeout,\n};\nsandbox.window.window = sandbox.window;\nconst context = vm.createContext(sandbox);\nvm.runInContext(source, context, { filename: 'tools/kanji-modernizer/app.js' });\nconst api = context.__test;\nassert.ok(api, 'modernizer test exports should be available');\n\nconst dict = api.rebuildDict({\n  old_to_new: { '舊': '旧', '學': '学', '體': '体' },\n  new_to_old: { '旧': ['舊', '舊'], '学': ['學'], '辺': ['邊', '邉'] },\n});\nassert.deepEqual(Array.from(dict.new_to_old['旧']), ['舊']);\n\nconst oldToNew = api.convertText('舊舊學𠮷', 'old-to-new', dict, { exclude: false });\nassert.equal(oldToNew.plain, '旧旧学𠮷');\nassert.deepEqual(oldToNew.replacements.map(item => [item.from, item.to, item.count]), [['舊', '旧', 2], ['學', '学', 1]]);\n\nconst conservative = api.convertText('辺辺', 'new-to-old', dict, { exclude: false, policy: 'conservative' });\nassert.equal(conservative.plain, '辺辺');\nassert.equal(conservative.ambiguities[0].action, 'preserved');\nassert.equal(conservative.ambiguities[0].count, 2);\nassert.deepEqual(Array.from(conservative.ambiguities[0].candidates), ['邊', '邉']);\nconst first = api.convertText('辺辺', 'new-to-old', dict, { exclude: false, policy: 'first' });\nassert.equal(first.plain, '邊邊');\nassert.equal(first.ambiguities[0].action, 'selected');\nassert.equal(first.ambiguities[0].resultChar, '邊');\n\nconst excluded = api.convertText('舊 https://example.com/舊 \\`\\`\\`學\\`\\`\\` 學', 'old-to-new', dict, { exclude: true });\nassert.equal(excluded.plain, '旧 https://example.com/舊 \\`\\`\\`學\\`\\`\\` 学');\nconst unexcluded = api.convertText('舊 https://example.com/舊', 'old-to-new', dict, { exclude: false });\nassert.equal(unexcluded.plain, '旧 https://example.com/旧');\n\nconst prepared = api.prepareInputText('  舊\\n學  ');\nassert.equal(prepared.hasContent, true);\nassert.equal(prepared.text, '  舊\\n學  ', 'conversion input must preserve leading/trailing whitespace and newlines');\nassert.equal(api.prepareInputText('  \\n  ').hasContent, false);\n\napi.setLang('ja');\nconst jaResult = api.convertText('舊學', 'old-to-new', dict, { exclude: false }).plain;\napi.setLang('en');\nconst enResult = api.convertText('舊學', 'old-to-new', dict, { exclude: false }).plain;\nassert.equal(jaResult, enResult, 'language switching must not alter conversion semantics');\n\nfetchImpl = async () => ({ ok: false, async json() { return {}; } });\napi.resetDictCache();\nawait assert.rejects(() => api.loadDict(), /Failed to load dict\.json/);\nfetchImpl = async () => ({ ok: true, async json() { return { old_to_new: { '舊': '旧' }, new_to_old: { '旧': ['舊'] } }; } });\napi.resetDictCache();\nconst loaded = await api.loadDict();\nassert.equal(loaded.old_to_new['舊'], '旧');\n\nconst referenceDict = JSON.parse(fs.readFileSync(new URL('../../old-kanji-reference/dict.json', import.meta.url), 'utf8'));\nconst modernizerDict = JSON.parse(fs.readFileSync(new URL('../dict.json', import.meta.url), 'utf8'));\nassert.deepEqual(modernizerDict, referenceDict, 'Reference and Modernizer parsed dictionaries must remain synchronized');\n\nassert.match(source, /let pendingAutoConvert = Boolean\(qParam\)/, '?q= must be queued until dictionary readiness');\nassert.match(source, /pendingAutoConvert = false;[\\s\\S]*Promise\.resolve\(\)\.then\(\(\) => \{ if \(!convertBtn\.disabled\) convertBtn\.click\(\); \}\)/);\nassert.doesNotMatch(source, /if \(qParam && convertBtn\) convertBtn\.click\(\);/, 'must not click a disabled converter before dictionary initialization');\nassert.match(source, /cleanUrl\.searchParams\.delete\("q"\)/, 'Reset should clear q handoff state from the URL');\n\nconst indexHtml = fs.readFileSync(new URL('../index.html', import.meta.url), 'utf8');\nassert.match(indexHtml, /正式表記や固有名詞の正確性は保証しません。/);\nassert.match(indexHtml, /It does not verify official names, context, or proper nouns\./);\n\nconsole.log('Kanji Modernizer behavior test passed.');\n`);

console.log('Old Kanji Wave 11 implementation/test patch applied.');
