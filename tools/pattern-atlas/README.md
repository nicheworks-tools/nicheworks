# Pattern Atlas

Pattern Atlas is a planned NicheWorks tool for exploring world patterns, editing colors, previewing tiles, and exporting SVG / PNG / CSS assets.

This directory is reserved for the Pattern Atlas implementation. All Pattern Atlas-specific files must remain under `tools/pattern-atlas/`.

See `docs/pattern-atlas-v0.1-spec.md` for the current implementation specification.
