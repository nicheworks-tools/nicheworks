(() => {
  "use strict";

  // Reviewed camera records that intentionally do not receive an accessory-detail handoff.
  // Keep this ledger empty until a model has been individually reviewed and an explicit
  // evidence-backed exclusion reason has been established.
  window.MANUALFINDER_CAMERA_DETAIL_EXCLUSIONS = Object.freeze([]);
})();
