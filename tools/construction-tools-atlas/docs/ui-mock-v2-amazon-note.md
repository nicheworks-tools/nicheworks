# UI Mock v2 Amazon note

The affiliate block appears only for canonicals with explicit maintained mappings. Search input is never used as an affiliate destination. The shared NicheWorks Amazon helper owns destination validation, sponsored-link semantics, disclosure rendering, and coarse click telemetry.
