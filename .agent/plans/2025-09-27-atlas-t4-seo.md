1. **Goal**
   - Add minimal SEO/OG/Twitter/canonical essentials plus 404/robots/sitemap for `tools/construction-tools-atlas/`, following the common spec v2 SEO expectations (spec-ja.md section 9-3) without changing tool behavior or layout.

2. **Scope**
   - targets: `tools/construction-tools-atlas/index.html`
   - targets: `tools/construction-tools-atlas/pages/about.html`
   - targets: `tools/construction-tools-atlas/pages/method.html`
   - targets: `tools/construction-tools-atlas/pages/disclaimer.html`
   - targets: `tools/construction-tools-atlas/pages/credits.html`
   - targets: `tools/construction-tools-atlas/404.html` (new)
   - targets: `tools/construction-tools-atlas/robots.txt` (new)
   - targets: `tools/construction-tools-atlas/sitemap.xml` (new)
   - excluded: everything else

3. **Rules / Prohibitions**
   - No cross-tool navigation; do not add header nav.
   - Do not edit anything outside `tools/construction-tools-atlas/`.
   - Do not modify `common-spec/spec-ja.md`.
   - Do not add external dependencies.
   - Keep existing language behavior; no new language switch unless already present.

4. **Change List**
   - Add/update `<title>`, meta description, viewport (if missing), canonical, OG, and Twitter tags on the specified HTML pages.
   - Use the assumed base URL `https://nicheworks.pages.dev/construction-tools-atlas/` for canonical and sitemap `<loc>`.
   - If an OG image exists in the tool folder, reference it; otherwise omit `og:image`.
   - Create `404.html` using the same header/footer styling and `style.css` as `index.html`, with links to `./index.html` and `./pages/about.html`.
   - Create `robots.txt` and `sitemap.xml` with the required entries.

5. **Step-by-step Procedure**
   1. Inspect existing HTML head sections and check for any existing OG images in the tool folder.
   2. Update head tags for SEO/OG/Twitter/canonical/viewport across the target pages.
   3. Add `404.html` with shared styling and appropriate links.
   4. Add `robots.txt` and `sitemap.xml` with absolute URLs.
   5. Review diffs to ensure minimal, in-scope changes only.

6. **Test Plan**
   - Open each HTML page in a browser and view source to confirm title/description/OG/Twitter/canonical/viewport tags.
   - Open `404.html` and verify styling and links back to index/about.
   - Open `robots.txt` and `sitemap.xml` to confirm exact contents and absolute URLs.

7. **Rollback Plan**
   - Revert the commit to restore previous HTML heads and remove newly added files.
