1. **Goal**
   - Build tools 01–05 under `tools/` from `_template`, updating SEO/canonical/JSON-LD and UI per MASTER.md specs.

2. **Scope**
   - targets:
     - `tools/analytics-privacy-kit/**`
     - `tools/og-image-maker/**`
     - `tools/lp-skeleton-generator/**`
     - `tools/pages-deploy-guide/**`
     - `tools/notion-form-design-kit/**`
   - excluded: everything else

3. **Rules / Prohibitions**
   - No cross-tool navigation beyond the allowed small footer links block in the template.
   - Do not edit files outside the scope.
   - Do not modify `common-spec/spec-ja.md` or other specs.
   - Keep JP/EN toggle and language rules as defined in the template.
   - Vanilla JS only; no external libraries.

4. **Change List**
   - For each tool, copy `_template` files and update:
     - `<title>`, meta description, canonical URL, OGP, JSON-LD fields.
     - Header tool name and lead (JP/EN).
     - Tool UI inside `#toolRoot` and tool-specific JS appended to `app.js`.
     - Ensure AdSense, ad slots, donate links remain.

5. **Step-by-step Procedure**
   1. Copy `_template` into each tool folder.
   2. Edit `index.html` placeholders for each tool.
   3. Add tool-specific UI markup inside `#toolRoot`.
   4. Append tool-specific logic to `app.js` after template utilities.
   5. Quick spot-check that JP/EN toggle and mobile layout remain intact.

6. **Test Plan**
   - Open each `tools/{slug}/index.html` in a browser.
   - Toggle JP/EN and confirm labels switch correctly.
   - Enter sample inputs and verify outputs render.
   - Check layout at 360px width.

7. **Rollback Plan**
   - Use `git restore` on the tool directories to revert changes.
