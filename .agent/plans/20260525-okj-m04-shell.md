1. Goal
   - Add a new static shell page for `tools/old-kanji-ocr-scanner/` with JP/EN UI, single-image input + preview, OCR placeholder, manual text fallback links, and required safety notes.

2. Scope
   - targets:
     - `tools/old-kanji-ocr-scanner/index.html`
     - `tools/old-kanji-ocr-scanner/style.css`
     - `tools/old-kanji-ocr-scanner/app.js`
   - excluded: all other files/folders

3. Rules / Prohibitions
   - Do not implement OCR, Tesseract.js, external OCR API, Pro gating runtime, Stripe, history/export/report features.
   - Do not modify SEO audit/sitemap/tools index/meta files or unrelated tools.
   - Reuse existing analytics/ad/cloudflare values from existing tools; do not invent token values.

4. Change List
   - Create `index.html` with required metadata, language switch, input/preview placeholders, OCR placeholder block, manual text fallback, notes, and related links.
   - Create `style.css` for responsive NicheWorks-style layout with no horizontal overflow on 360px.
   - Create `app.js` implementing required functions for language switching, image preview lifecycle, manual-link updates, and safe DOM updates.

5. Step-by-step Procedure
   1) Inspect nearby tools for standard head/footer snippets and analytics/ad/cloudflare tag values.
   2) Implement `index.html` shell with bilingual strings via `data-i18n` attributes and the required sections.
   3) Implement `app.js` with required named functions and event wiring.
   4) Implement `style.css` responsive styles.
   5) Run `node --check tools/old-kanji-ocr-scanner/app.js` and verify git diff scope.
   6) Commit and prepare PR body with manual verification checklist.

6. Test Plan
   - Run `node --check tools/old-kanji-ocr-scanner/app.js`.
   - Confirm only target files changed via `git status --short`.

7. Rollback Plan
   - Revert commit or remove `tools/old-kanji-ocr-scanner/` and restore branch state before this change.
